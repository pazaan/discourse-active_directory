en:
  site_settings:
    authad_domain_controller: "Domain Controller"
    authad_base_dn: "Base DN (i.e. 'dc=example,dc=com')"
    authad_bind_dn: "Unprivileged user to perform user lookups (i.e. DN=limited_account,CN=Users,DC=example,DC=com)"
    authad_bind_pass: "Password for unprivileged user"
